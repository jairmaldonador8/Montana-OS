{
  "name": "montana-os-monorepo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "next": "14.2.18"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.14",
    "postcss": "^8.4.49",
    "autoprefixer": "^10.4.20"
  },
  "scripts": {
    "build": "cd montana-os && npm run build",
    "dev": "cd montana-os && npm run dev",
    "start": "cd montana-os && npm start",
    "lint": "cd montana-os && npm run lint",
    "type-check": "cd montana-os && npm run type-check",
    "test": "cd montana-os && npm test"
  }
}
